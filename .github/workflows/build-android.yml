
name: Build Android APK

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 设置 JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 安装依赖
        run: npm ci --legacy-peer-deps

      - name: 设置 Android SDK
        uses: android-actions/setup-android@v3

      - name: 创建签名配置
        run: |
          cd android/app
          keytool -genkeypair -v -storetype PKCS12 \
            -keystore release.keystore \
            -alias release-key \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass android -keypass android \
            -dname "CN=BillTracker, OU=App, O=Company, L=City, ST=State, C=US"

      - name: 添加Gradle包装器JAR
        run: |
          cd android
          mkdir -p gradle/wrapper
          curl -L -o gradle/wrapper/gradle-wrapper.jar https://raw.githubusercontent.com/gradle/gradle/master/gradle/wrapper/gradle-wrapper.jar

      - name: 构建 Android APK
        run: |
          cd android
          chmod +x ./gradlew
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: 上传 APK
        uses: actions/upload-artifact@v4
        with:
          name: BillTrackerApp-release
          path: android/app/build/outputs/apk/release/app-release.apk

      - name: 创建 Release
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          files: android/app/build/outputs/apk/release/app-release.apk
          body: |
            📱 自动构建的记账APP - 支持OCR账单识别
            
            **版本信息**
            - 构建编号: ${{ github.run_number }}
            - 提交: ${{ github.sha }}
            - 构建时间: ${{ github.event.head_commit.timestamp }}
            
            **安装说明**
            1. 下载 `app-release.apk`
            2. 在Android设备上开启"未知来源"安装权限
            3. 安装APK文件
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
